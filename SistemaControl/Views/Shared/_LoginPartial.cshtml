@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("Logoff", "Login", FormMethod.Post, new { id = "logoutForm"/*, @class = "navbar-right" */}))
    {
        @Html.AntiForgeryToken()
        @*<div class="ui dropdown item" style="color:white;">
                @User.Identity.Name <i class="dropdown icon"></i>
                <div class="menu">
                    <a class="item" href="javascript:document.getElementById('logoutForm').submit()">Cerrar sesión.</a>
                    <a class="item" href="~/Home/About">Acerca SCJ</a>
                </div>
            </div>*@
        <div class="navbar-custom-menu">
            <ul class="nav navbar-nav">
                <li class="dropdown user user-menu">

                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                        <!-- The user image in the navbar-->
                        @*<img src="~/Content/img/Courthouse.png" class="user-image" alt="User Image">*@
                        <i class="fa fa-user-circle" alt="User Image"></i>
                        <!-- hidden-xs hides the username on small devices so only the image appears. -->
                        <span class="hidden-xs">  @User.Identity.Name </span>
                    </a>
                    <ul class="dropdown-menu">
                        <!-- The user image in the menu -->
                        <li class="user-header">
                            <img src="~/Content/img/Courthouse.png" class="img-circle" alt="User Image">
                            <p>
                                Sistema de Control Jurídico
                                <small>Municipalidad de Alajuela</small>
                                <small>Procesos Servicios Jurídicos</small>
                            </p>
                        </li>
                        <!-- Menu Body -->
                        <li class="user-body">
                            <div class="row">
                                <div class="col-lg-auto text-center">
                                    <p>Universidad Nacional de Costa Rica</p>
                                </div>
                            </div>
                            <!-- /.row -->
                        </li>
                        <!-- Menu Footer-->
                        <li class="user-footer">
                            <div class="pull-left">
                                <a href="javascript:document.getElementById('logoutForm').submit()" class="btn btn-default btn-flat">Cerrar sesión.</a>
                            </div>
                            <div class="pull-right">
                                <a class="btn btn-default btn-flat" href="~/Home/About">Acerca SCJ</a>
                            </div>
                        </li>
                    </ul>
                </li>
            </ul>
        </div>
    }
}
else
{
    <div class="navbar-custom-menu">
        <ul class="nav navbar-nav">
            <li class="dropdown user user-menu">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                    <!-- The user image in the navbar-->
                    @*<img src="~/Content/img/Courthouse.png" class="user-image" alt="User Image">*@
                    <i class="fa fa-exclamation-triangle" alt="User Image"></i>
                    <!-- hidden-xs hides the username on small devices so only the image appears. -->
                </a>
                <ul class="dropdown-menu">
                    <!-- Menu Footer-->
                    <li class="user-footer" style="background:#7fbeeb">
                        <a href="/Login/Index" btn btn-default btn-flat">Iniciar sesión</a>
                    </li>
                </ul>
            </li>
        </ul>
    </div>
}
